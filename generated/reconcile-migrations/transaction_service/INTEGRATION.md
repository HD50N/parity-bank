# Integrating the REST API: **TransactionService**

The OpenAPI contract is committed as [`openapi.yaml`](./openapi.yaml) (repo path: `generated/reconcile-migrations/transaction_service/openapi.yaml`). This guide explains how to connect **TransactionService** to the rest of the codebase. Wiring hints were read from the `main` branch at PR generation time.

## Legacy surface (today)

- **Service module in repo:** `services/transaction_service.py`
- **Stack:** Python / SOAP
- **Legacy route / SOAP-style endpoint:** `/transaction`
- **Operations:** `GetTransactionHistory`

## What to change in *this* codebase

1. **Keep shipping while migrating** — expose the REST surface behind a new path prefix (for example `/api/v2/...`) and leave the SOAP handler in place until callers switch.
2. **Implement the contract** — use the OpenAPI file in this PR as the source of truth:
   - Hand-written Flask/FastAPI routes that mirror the paths and schemas, **or**
   - `openapi-generator` / `datamodel-code-generator` to stub models and handlers, then fill in business logic by delegating to the same domain layer the SOAP stack uses today.
3. **Deprecate SOAP intentionally** — once REST traffic passes your parity tests, gate the legacy endpoint behind a feature flag or return `410 Gone` with a migration URL.

### Repository wiring (auto-detected)

These paths look related to how the app is bootstrapped or how services are registered. Review them before editing generated-only files.

### `server.py`

First notable line contains “flask” (line 1 in file).

```text
from flask import Flask, send_from_directory
from services.account_service import account_bp
from services.transaction_service import transaction_bp
from services.transfer_service import transfer_bp
from services.customer_service import customer_bp

app = Flask(__name__)

@app.route("/")
def index():
    return send_from_directory("frontend", "index.html")
app.register_blueprint(account_bp, url_prefix="/account")
app.register_blueprint(transaction_bp, url_prefix="/transaction")
app.register_blueprint(transfer_bp, url_prefix="/transfer")
app.register_blueprint(customer_bp, url_prefix="/customer")

if __name__ == "__main__":
    print("Parity Bank SOAP API â http://localhost:8000")
    print("  GetAccountBalance:     GET  http://localhost:8000/account?wsdl")
    print("  GetTransactionHistory: GET  http://localhost:8000/transaction?wsdl")
    print("  TransferFunds:         GET  http://localhost:8000/transfer?wsdl")
    print("  GetCustomerInfo:       GET  http://localhost:8000/customer?wsdl")
    app.run(host="0.0.0.0", port=8000, debug=False)

```

### `requirements.txt`

First notable line contains “flask” (line 1 in file).

```text
flask==3.0.0

```


## Concrete checklist

- [ ] Map each legacy operation to the new REST path + verb from the OpenAPI spec.
- [ ] Reuse validation and persistence code from the SOAP handler where possible (avoid duplicating business rules).
- [ ] Add integration tests that hit the REST routes and compare responses to the SOAP baseline (golden files or recorded fixtures).
- [ ] Update deployment config (ports, reverse proxy, health checks) if the REST app listens on a new process or base path.

---

_Guide path: `generated/reconcile-migrations/transaction_service/INTEGRATION.md` · Generated by Reconcile_
